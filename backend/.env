# Configuração do Banco de Dados
# Substitua os valores abaixo pelos da sua configuração local do MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=''
DB_NAME=avaliacao_educacional

# Segredo para gerar os tokens de autenticação (JWT)
# Substitua por uma frase longa, aleatória e segura
JWT_SECRET=este-e-um-segredo-temporario-troque-por-algo-forte